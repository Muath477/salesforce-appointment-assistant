# Outstanding: Einstein Bot screenshots

The `Screenshots/` folder currently has 11 images (01–11) covering the data
model, Flows, Screen Flow, Lightning App, and reports/dashboard. The Einstein
Bot section added in README.md / `docs/AGENTFORCE.md` §7.1 / the .docx and
.pptx still needs its own visual evidence, captured directly from Bot Builder:

1. **Setup → Einstein Bots → My Bots list** — showing "Appointment Assistant",
   Type = Enhanced, Status = **Active**.
2. **Bot Builder → Dialogs list** (left sidebar) — showing all dialogs,
   including Schedule / Reschedule / Cancel an appointment.
3. **Bot Builder → "Schedule an appointment" dialog canvas** — the
   question/action blocks.
4. **Bot Builder → "Reschedule an appointment" dialog canvas**, scrolled down
   to show the "Run Flow action: Reschedule Appointment" block.
5. **Bot Builder → "Cancel an appointment" dialog canvas**, scrolled down to
   show the "Run Flow action: Cancel Appointment" block and the "Your
   appointment has been cancelled!" message.

Once captured, add them to `Screenshots/` with these filenames (continuing the
existing 01–11 numbering) and this file can be deleted:

- `12_Einstein_Bot_Active_Status.png`
- `13_Bot_Builder_Dialogs_List.png`
- `14_Bot_Schedule_Dialog.png`
- `15_Bot_Reschedule_Dialog.png`
- `16_Bot_Cancel_Dialog.png`

They should also be added to the gallery grid in `index.html` (`#galleryGrid`)
and to the `Screenshots/` sections referenced in the .docx / .pptx once
available.
